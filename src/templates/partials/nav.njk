<!-- nav.njk -->
<nav id="main-menu" class="right">
  <div class="menu-section">
    {% for section in sections %}
    <a href="#{{section.id}}" class="section-link">
      <i class="fa {{section.menuIcon}}"></i>
      {{section.label}}
    </a>
    {% endfor %}
  </div>
</nav>
